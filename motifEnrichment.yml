
RUN_DIR: "MotifEnrichment/singleLevelRunOnlyUTR"

Fasta: "initialData/Nostocsp.fasta"  # Genome FASTA file; must match the organism configured in your DESeq config

multilevel: False

DESeqConfig:
  file: "config.yml"


motifEnrichment:
  lfc_cutoff: 2
  padj_cutoff: 0.05
