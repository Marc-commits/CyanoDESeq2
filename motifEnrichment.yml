---
RUN_DIR: "MotifEnrichment/singleLevelRunOnlyUTR"

# Genome FASTA file; must match the organism configured in your DESeq
# config
Fasta: "initialData/Nostocsp.fasta"

multilevel: false

DESeqConfig:
  file: "config.yml"

motifEnrichment:
  lfc_cutoff: 2
  padj_cutoff: 0.05
